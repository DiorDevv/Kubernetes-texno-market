# prometheus.yml — Prometheus konfiguratsiyasi

global:
  scrape_interval:     15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'texno-market'
    static_configs:
      - targets: ['app:8000']
